# NATIVE DESKTOP — MERGED INTO `gospel-goal.md` (2026-08-09)

**This doc is a stub. Do not add items here. Do not treat it as a
tracker.** There is one goal doc: **`docs/gospel-goal.md`**.

Written 2026-08-09 to make gospel Track N runnable, and merged into
gospel the same day — it existed as a separate file for about an hour,
which is the correct lifespan for a second goal doc. Everything is now
in gospel "TRACK N — NATIVE DESKTOP":

- The three tool-satisfiable gates (N0 port passport / N2 playable
  offline / N3 first-class client) and the scope line ("no TS" means the
  desktop *artifact*; de-TS-ing the browser client and Bun server is
  Track E3).
- The measured starting position — sim size, the 54,392-line Phaser port
  surface, bot brain, map gen, delta codec, world-init gap, `.jjr`
  format, asset weight.
- Phases N0, N1, N2, N3, N4 with every item; the interlock lanes N-BOT /
  N-MAP / N-AIM; the new N-PLATFORM target-order section.
- Laws NL0–NL6 → unified into gospel's single law list as **L9** (the
  shell is not the sim), **L10** (hash-first determinism), **L11**
  (baked tier first), **L12** (decisions are spiked, not vibed) and
  **L13** (this box bites). NL5's single-writer files folded into L5;
  NL0 was deleted as redundant (it only said "gospel's laws apply").
- The native meters → gospel Track P's engine-meter list.

The N1 shell decision keeps its own home, as decision records should:
**`docs/adr/0008-native-shell-raylib.md`** — raylib, SDL3 as named
fallback, alternatives and switch triggers recorded.
