# Legibility audit — every live effect's world-space read (site, not nameplate)

*Generated 2026-07-23 by the convergence-goal Track L audit workflow (17 agents,
79 effects enumerated from six-axes Layer 1 + starter actives + all four class
catalogs + statuses + emission). Doctrine #10: "at every point it should be
clear what's going on" — a nameplate chip is NOT a site read. Regenerate by
re-running the workflow; hand-edit only the Fix-plan column.*

**Verdicts: 79 rows — 0 MISSING, 14 PARTIAL, 65 SHIPPED.**
MISSING and PARTIAL rows ARE the Track L work list (cohesion-goal.md P3.3's
"every row either names a shipped read or becomes a work item IN the table").

| Effect | Verdict | Event | Presenter | Note |
|---|---|---|---|---|
| stride.dashReset (Stride refund) | **SHIPPED** | stride-refunded (World.ts:3078, emitted only when air movement was actually spent — a no-op reset stays silent) | client/src/game/systems/StatusVfxController.ts:156 → spawnStrideRefundSweep (def :174) — upward-sweeping pair of flattened cyan rings rising from the FEET up the body + two rising tick sparks (movement register, the exact inversion of slow's inward drag wake; STRIDE_COLOR 0x67e8f9); routed via SimEventRouter.ts:409 (deliberately silent — never-synthesize rule; the same-tick emission-cast carries the audio) | Track L 2026-07-23: the refund lives only in host movement memory, so the additive stride-refunded SimEvent is the sole possible renderer feed — sim-tested in axisCastEffects.test.ts (event on a spent-counter stride cast; silent on a no-Stride hand AND on a stride hand with nothing spent). Registry row at eventPresentationRegistry.ts ("stride-refunded", complete). Distinct from the axis-blind emission-cast seal by construction. |
| technique.executeBelowFrac (Technique execute) | **SHIPPED** | player-killed with additive `executed: true` (World.ts execute raise site — flag only survives onto the event when the hit really killed, so a team-peel save never wears it) | client/src/game/render/deathFxPainter.ts:138 — a single clean horizontal severance shear through the soul at the unmake moment (crisp instrument-stroke travelling outward + hairline parting echo; all fx tiers, drawn outside the fx1+ dissolve gate; deliberately subtler than ascension-denial); threaded event→soul→model via renderContract.ts:562 (noteDeathEvents) → SoulRenderModel.executed :421 | Track L 2026-07-23: follows the ascension-denial precedent (renderContract soul threading + deathFxPainter mark) but EVENT-threaded rather than snapshot-derived — the raise depends on the victim's transient pre-hit health, which no snapshot retains. Sim-tested (axisCastEffects.test.ts: executed=true on the 14% execute kill, absent on an ordinary lethal shard) and derivation-tested (deathFx.test.ts: event→soul→model threading, absent-flag default, recycled-soul reset). Registry: player-killed note updated. |
| aegis-share | **SHIPPED** | ability-activated (kind "aegis-share", World.ts:4515-4522) for the cast beat; sustained window state-driven (aegisShareUntilTick — ConstructVfxController aura + statusWindowPlan reach ring) | mechanic read: client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnAegisReachRing (def :601) — a thin Kindled-gold ring breathing at the TRUE widened peel radius (WARD_PEEL_RADIUS_PX × KIN_AEGIS_SHARE_RADIUS_MULTIPLIER = 256px, imported from the sim so the drawing can never lie) every 400ms, 0.16 alpha — an instrument line, not a dome; identity aura: ConstructVfxController.ts:940 drawBuffAura (unchanged); peel payoff: team-peel-absorbed → spawnWardAbsorb + heavy hit-stop (unchanged); cast: rig 'guard' gesture + card SFX | Track L 2026-07-24: closes the row's named gap — "the widened peel radius is never drawn". Drawing the true radius also solves the aegis-vs-kindled-resolve ambiguity from the outside (only aegis wears the reach ring), without touching the shared CVC aura. Recorded caveats: the solo Kindling-feed fallback (World.ts:3596-3603) still has no dedicated site read, and the peel payoff remains generic team-peel presentation rather than aegis-attributed — both CVC/World-side, deferred. Planner tested (statusWindowPlan.test.ts). |
| bastion-pulse | **SHIPPED** | ability-activated (cast floor) + shield-refunded (World.ts:3631 — additive, emitted only when charge actually ROSE; ward-held doubling reads through the event amount) | client/src/game/systems/StatusVfxController.ts:312-313 → spawnShieldRefundSnap (def :642) — a fast sapphire ring SNAPPING onto the vessel + one rising charge tick (the shield bar's own hue; read-distinct from the ward shell's slow steady pulse by tempo). Plus the unchanged generic floor: 'pulse' rig gesture, class cast tell (ConstructVfxController.ts:609), card SFX. | Track L 2026-07-24: the actual effect — instant shield-charge refund, doubled while Ward is held — now has a dedicated site read at the vessel (was HUD-bar-only). Honest by construction: a full-bar cast emits nothing (sim-tested, legibilityEvents.test.ts: emission with the real post-cap amount, ward-held doubling asserted, full-bar silence via the Return Glass twin). Registry row "shield-refunded" complete; deliberately silent audio (never-synthesize; the press's card cue carries it). |
| blockJammer (blockJammerUntilTick) | **SHIPPED** | state-driven via render/statusWindowPlan.ts (no SimEvent exists — pickup.ts's application stays silent, and none is needed: the window read appears the frame the field lands) | client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnJamSputter (def :547) — violet sputter pips fizzing at the body every 280ms while jammed (electrical register, matches the JAM chip's violet; short snappy 150ms transients read as shorting electronics), 300ms expiry fade | Track L 2026-07-24: was a zero-world-read row — the victim's shield vanished silently and dead block presses had no explanation. The jam now reads at the body for its whole window, for the victim AND for attackers deciding to press the advantage. Recorded caveats: the application MOMENT (pickup detonation) still emits no dedicated event (the window read appearing is the application read), and a jammed block ATTEMPT has no extra fizzle beat — both would need sim-side event emission at pickup.ts, out of this pass's additive-World.ts-only scope. Planner tested (statusWindowPlan.test.ts). |
| borrowed-time | **PARTIAL** | ability-activated (cast only); debt landing is state-driven (frame-diff — World.ts §1b drain ~L5984 emits no event; ally write via pendingSyzygistCasts ~L4588 also silent) | client/src/game/systems/ConstructVfxController.ts:893-894 (spawnNovaBurst SYZYGIST 'ooze' debt burst, frame-diffed debtUntilTick) | The debt LANDING has a real dedicated site read — an ooze-style nova burst on whoever the drain actually hits (self or healed ally; per-player scan covers the cross-player case). But the heal moment has no read at the recipient's body (cast tell + 'mark' gesture fire at the CASTER; no heal number — damageNumber.ts has no heal path — and the buff-aura block covers regen/haste/ward, not this), and the 6s pending-debt window is nameplate-only (statusChips.ts:192 DEBT chip in HUD/ActionBar strips). An ally silently gains HP, then 6s later bursts — the front half of the ability is invisible in-world. |
| contagion | **PARTIAL** | ability-activated kind 'contagion' (only emitted when a jump occurred); the jump itself is a silent state write (World.ts:4600-4628, no SimEvent) | client/src/game/systems/ConstructVfxController.ts:609 (generic caster cast-tell only); jump-site draw: NONE | Caster-side only: rig 'weave' gesture (SimEventRouter.ts:443, abilityAnimation.ts:83), generic cast-tell burst (ConstructVfxController.ts:598-610) and 'card' SFX (SimEventRouter.ts:438). The signature moment — the burn copying onto the jump target — has NO dedicated read at either the source or the jump target: World.ts:4613-4627 writes burnUntilTick/burnDps silently, and the new burn only surfaces via the generic state-driven burn sparks any burn gets (StatusVfxController.ts:67-76, spawnBurnSpark :72). No source-to-target arc/tendril exists (chain-arc language exists for chain-hit/emission-leech but is not wired to contagion). |
| counter-stance (counterUntilTick) | **SHIPPED** | ability-activated (kind=severing-answer) at cast; parry-deflected on consumption; window state-driven via render/statusWindowPlan.ts | window: client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnCounterStanceTicks (def :364) — paired amber pincer ticks cocked INWARD at chest height on a fast 90ms beat (the whole stance is ~500ms, so the read must land instantly; 150ms fade tail keeps most of the window at full intensity), so an attacker can SEE the trap before shooting into it. Payoff: SimEventRouter.ts:324-337 parry-deflected flash + heavy hit-stop at the stancer, returned damage via hit-confirmed at the attacker (unchanged). Cast: ConstructVfxController.ts:609 tell + 'cut' gesture. | Track L 2026-07-24: closes the row's core gap — "an opponent cannot see the stance is up before shooting into it" — with a dedicated armed-stance body read (planner tested, statusWindowPlan.test.ts incl. the short-fade contract). Recorded caveat (unchanged): the consumption still borrows the parry vocabulary wholesale; a bespoke severing identity for the payoff flash lives in rig/parry territory (ProceduralPlayerRig — currently melee-loop-owned). CNTR chip supplementary. |
| double-jump (Kindled stomp-jump ring) | **PARTIAL** | state-driven (no SimEvent — World.ts ~2670 writes 6 shards straight into projectilesCow, no events.push) | client/src/game/render/ProjectileVfx.ts:297 (generic drawBody; muzzle :207, halo :267, despawn impact/fizzle diff ~:320) | No dedicated presentation anywhere: no stomp/ring VFX, no SFX (audio rides shot-fired, which never fires here), no jump-site burst, no rig gesture (movement passive, not ability-activated). The ring only reads because the 6 radius-6 shards are real projectiles picked up by the generic state-driven renderer (muzzle flash + trail + halo + fizzle at ~70px range, ~320ms life), wearing the caster's build shape/element — indistinguishable from ordinary shots and completely silent. |
| E-coupling self-veil (mystery.markMs → veilUntilTick on cast, Veil of Nought held) | **SHIPPED** | state-driven (veilUntilTick in snapshot — render/veilReadPlan.ts reads the field regardless of WHICH cast set it, so the E-coupling path rides the veil-of-nought shroud by construction) | client/src/game/systems/StatusVfxController.ts:178-192 planVeilRead → spawnVeilShroud (def :392) while veiled + spawnVeilBreakSeam (def :420) on break-by-firing — the same world read as the drafted active | Track L 2026-07-24: verified stale row — the veil-of-nought shroud (see that row) landed 2026-07-23 reading veilUntilTick state-driven, which covers the E-coupling self-veil identically (planner tested, veilReadPlan.test.ts). Remaining nuance (recorded, not a blocker): the coupling CAST moment itself still has only the generic emission seal flash (the 'veil-of-nought' rig gesture fires only on the drafted active's ability-activated), but the sustained window + break now read in-world at the body. VEIL chip is supplementary. |
| E-coupling stride surge (speedBoostUntilTick on cast, Shadow Step held) | **SHIPPED** | state-driven (speedBoostUntilTick in snapshot via render/statusWindowPlan.ts — the planner reads the field regardless of source, so pickup/card speed boosts read identically) | client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnSurgeStreaks (def :492) — horizontal stride-cyan motion streaks peeling off BEHIND the mover at body height every 150ms (movement register, the hotter stride cyan — distinct from slow's pale foot-level drag rings and the stride-refund feet sweep), trailing the actual velocity sign (both sides when stationary), 300ms expiry fade | Track L 2026-07-24: the surge window now reads on the fighter in-world for its whole duration (was SPD-chip-only). The cast-moment feel remains element-keyed emission presentation (recorded, acceptable — the sustained window is the fairness-relevant read: "why is that fighter suddenly faster" now has a visible answer). vxSign behavior planner-tested (statusWindowPlan.test.ts). |
| emission radiant fork — 1.4× amp vs already-statused target | **SHIPPED** | hit-confirmed with additive `amped: true` (World.ts resolveRangedHit radiant branch — stamped exactly when the 1.4× applied, so the flag can never lie about the math; Fooled's victim-state amp rides the same flag) | client/src/game/systems/StatusVfxController.ts:315-318 → spawnAmpPunishBurst (def :690) — a rose crack-burst at the victim (four radial ticks splitting outward from the body: the cracked guard being exploited; vuln/fooled family hue) layered ON TOP of the generic hit stack (damage number + element impact burst stay the lead) | Track L 2026-07-24: the amp was previously invisible IN PRINCIPLE — no event marker existed, so no renderer could distinguish an amped radiant hit. Now the punish-the-statused moment reads at the victim site for both players. Sim-tested (legibilityEvents.test.ts: amped=true + damage 28 on the statused hit, flag absent + damage 20 on the clean hit). Registry: hit-confirmed note updated. Distinct SFX still deliberately absent (never-synthesize rule; the visual layer is the read). |
| emission void fork — shield-piercing (voidPiercing) + wrap/denial | **PARTIAL** | denial: state-driven (renderContract.ts:602-603 sets soul.denied from killer build mystery.denyAscension, stage SOUL_DENIED at :724); pierce: hit-confirmed with no pierce marker (combat.ts:684-697 returns pass-through, emits nothing) | denial: client/src/game/render/deathFxPainter.ts:48 (denied soul — inverted D_CORE/D_INNER/D_HALO palette, sinking void stain + falling motes L106-118, ascending pillar suppressed); wrap: ProjectileVfx.ts:234-240 (trail-discontinuity guard only); pierce: NONE | One of three sub-reads is genuinely shipped: ascension-denial has a full inverted death read at the corpse site. Wrap has only a negative-space guard (breaks the trail so the teleport doesn't smear); the position jump itself is the only read. Shield-piercing has zero tell — the shield visual stays up, damage lands as a generic hit, and no event/flag ever says 'pierced', so the counter-pick moment is illegible to both players. |
| facet-break mark (facetTargetId/facetMarkUntilTick) | **SHIPPED** | hit-confirmed (amp consumption, state-driven mark check) + ability-activated kind:"facet-break" (cast); mark WINDOW state-driven (render/markReadPlan.ts inverts the attacker-side fields onto the target) | window: client/src/game/systems/StatusVfxController.ts:201-215 planMarkReads → spawnFacetMarkChords (def :287) — three flat Geometrician-cyan crystal chords precessing around the MARKED body every 240ms for the whole 4s window, intensity fading over the final 300ms (expiry tell); payoff: ConstructVfxController.ts:587 spawnEmpoweredHitFlourish at victim (unchanged); cast: rig gesture SimEventRouter.ts:443 + cast tell ConstructVfxController.ts:609 | Track L 2026-07-24: the mark window now reads at ITS site — the marked fighter visibly wears the hunter's instrument (doctrine #10 fairness: you can SEE who is primed for the amp before it lands), and expiry fades instead of popping. Chassis law kept: GEO cyan; the crystal-chord SHAPE distinguishes it from the Interstice read slashes (CA2 — GEO/INT share hue, differ by shape). Planner headless-tested (markReadPlan.test.ts: at-target read, stack indices, expiry fade, dead hunter/target silence). MARK chip stays supplementary. |
| fooled (fooledUntilTick) | **SHIPPED** | state-driven window (render/statusWindowPlan.ts — the read appears the frame the silent World.ts:4896-4908 write lands, no event needed); consumption via hit-confirmed `amped: true` (World.ts fooledDamageMultiplier site stamps the flag whenever the multiplier actually applied) | window: client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnFooledDouble (def :575) — a pink offset "double" of the body's silhouette sliding away to alternating sides every 320ms (the read on this fighter is a lie), 300ms expiry fade; consumption: spawnAmpPunishBurst at the victim on each amped hit (StatusVfxController.ts:315-318) | Track L 2026-07-24: application (the window read appearing at burst time), standing debuff, and every amped consumption now read at the body (was FOOL-chip-only with zero consumption tell). Sim-tested (legibilityEvents.test.ts: amped=true on a neutral-element hit vs a fooled victim, damage > base). NOTE the Paper Double decoy ENTITY's missing renderer is the paper-double row's item, not this one — deferred: melee-loop-owned (needs ProceduralPlayerRig). |
| glass-ward | **PARTIAL** | ability-activated kind 'glass-ward' (cast); syz-ward-absorbed (spend); ally application is a silent deferred state write (World.ts:4579-4585 → applyWardToAlly), no event | client/src/game/scenes/OnlineMatchScene.ts:1838-1851 (absorb blast at warded body + 18px source pulse at caster); persistent shell only via priest-gated aura (ConstructVfxController.ts:947-951) | Spend moment is a real site read for both branches: cool-white blast at the protected player with break escalation plus a separate source pulse at the caster so ally saves stay attributable (OnlineMatchScene.ts:1838-1852), heavy hit-stop/shake (SimEventRouter.ts:562-577). The gap is the ward's PRESENCE on the design-peak ally branch: the buff-aura scan gates wardAbsorbUntilTick under cls==='priest' (ConstructVfxController.ts:942/947), so a non-priest warded ally (World.ts:4051-4059) shows nothing at their body between cast and first hit — nameplate-only WARD chip (statusChips.ts:125) — and the ally application itself has no bind burst/thread (silent World.ts:4583 write). Self-fallback (always the case in current FFA, findNearestAlly null) does get the aura since the caster is a priest. Cast: rig 'weave' gesture (abilityAnimation.ts:86) + cast tell (ConstructVfxController.ts:609). |
| haste (hasteUntilTick/hasteMultiplier) | **PARTIAL** | state-driven (aura reads snapshot hasteUntilTick); cast cue rides generic 'ability-activated' kind=haste-gift (SimEventRouter.ts:420-449, caster-side audio+rig gesture only) | client/src/game/systems/ConstructVfxController.ts:944-951 (drawBuffAura → auraLayerPriest; drawBuffAura impl at client/src/game/render/LightConstruct.ts:2025) | A real world-space read exists — a continuous SYZYGIST_TINT 'ooze' buff-aura pulsing at the buffed fighter's body (ConstructVfxController.ts:944-951, world-space additive Graphics layer at AURA_DEPTH). But it is ambient-only and doubly weak: (1) the identical aura (same tint/shape/radius via max(haste, wardAbsorb) intensity) also means ward-absorb, so the two statuses are indistinguishable; (2) it is gated on resolveClassId(p.characterId)==='priest' (line 942), so ally-cast Haste Gift on a non-priest ally (applyHasteToAlly, World.ts:1051 via pendingSyzygistCasts World.ts:4575) renders NOTHING at the recipient — contradicting the block comment at L898-905. Plus nameplate-only HASTE chip statusChips.ts:115. Application moment at the ally has no burst; only the caster gets the generic 'card' cue. |
| judgment mark (judgmentTargetId/judgmentMarkUntilTick) | **SHIPPED** | slash-hit (amp consumption at paladin melee, state-driven mark check) + ability-activated kind:"judgment-line" (cast); mark WINDOW state-driven via render/markReadPlan.ts | window: client/src/game/systems/StatusVfxController.ts:201-215 planMarkReads → spawnJudgmentMarkArc (def :322) — a single partial Kindled-gold arc orbiting the marked quarry each 240ms, tightening as it fades (a seal instrument closing on the body — deliberately never a full halo ring, instrument-vs-icon hard line), for the 3s window with a 300ms expiry fade; payoff: ConstructVfxController.ts:551 spawnEmpoweredHitFlourish (unchanged); cast: SimEventRouter.ts:443 gesture + ConstructVfxController.ts:609 tell | Track L 2026-07-24: the marked target now carries a gold in-world read for the whole window, expiring visibly instead of silently. The payoff flourish still shares its visual with sealUntilTick (recorded caveat), but the WINDOW read is judgment-distinct by construction — only this mark wears the gold arc. Planner tested in markReadPlan.test.ts. |
| judgment-line | **SHIPPED** | slash-hit (payoff, + live snapshot judgmentTargetId/judgmentMarkUntilTick); ability-activated (cast); window state-driven via render/markReadPlan.ts | Window: StatusVfxController.ts:201-215 → spawnJudgmentMarkArc (def :322) — partial gold arc orbiting the MARKED enemy for the 3s window (see judgment-mark row). Payoff: ConstructVfxController.ts:551 (spawnEmpoweredHitFlourish at victimPos). Cast: SimEventRouter.ts:443 gesture + ConstructVfxController.ts:609 cast tell. | Track L 2026-07-24: closes this row's named gap ("no marker over the marked enemy for the 3s window") — the mark now reads at the target's site continuously, with an expiry fade. Cast remains the generic floor ('cut' rig gesture + shared tell; acceptable — the WINDOW and PAYOFF are the fairness-critical beats and both read in-world now). Caster-side MARK chip is supplementary. |
| kindled-resolve | **PARTIAL** | ability-activated for the cast (only emitted on the affordable branch — insufficient Kindling is a dead press, World.ts:3713-3722); sustained window is state-driven (kindledResolveUntilTick) | ConstructVfxController buff-aura scan → drawBuffAura(auraLayerPaladin, KINDLED_TINT, "seal", radius 22) at client/src/game/systems/ConstructVfxController.ts:940 (window read at :936-938); cast beat = rig "gather" gesture via SimEventRouter.ts:443 w/ contract abilityAnimation.ts:72, card SFX SimEventRouter.ts:438 | In-world aura exists but is byte-identical to aegis-share's (same KINDLED_TINT/"seal"/radius 22) and its cast verb ("gather") is shared with rally-light — nothing on-site identifies this as kindled-resolve. None of its actual effects present: the Kindling spend has no site flourish (contrast CVC's Second Wind/Borrowed Time consumption bursts), the kindledResolveDamageMultiplier amp on landed hits and applyKindledResolveStaggerResist saves have no distinct read at resolution, and there is no nameplate chip either (statusChips.ts has AEGIS but no kindled-resolve row — so this one is weaker than aegis even on the HUD). |
| measure | **SHIPPED** | ability-activated (cast); window state-driven via render/statusWindowPlan.ts (measureUntilTick — previously read by NO render/UI file at all) | window: client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnMeasureCalipers (def :462) — two thin Geometrician-cyan caliper ticks at eye level converging toward the head every 240ms (calibration closing to zero spread — the precision register, an instrument not a glow), 300ms expiry fade; cast: ConstructVfxController.ts:609 tell + rig 'mark' gesture + card SFX (unchanged) | Track L 2026-07-24: was the catalog's least legible window — invisible to everyone INCLUDING the caster (no aura, no chip, no activeSlots branch). The perfect-accuracy window now reads at the body for its whole life, for caster and watching enemies alike. Planner tested (statusWindowPlan.test.ts). A nameplate chip remains deliberately un-added — the site read is the read; chips are supplements, and this pass's job was the site. |
| paper-double | **PARTIAL** | ability-activated (kind="paper-double") for the cast; decoy entity is state-driven (state.paperDoubles via snapshotDelta) with NO renderer; burst damage rides hit-confirmed at victims | client/src/game/render/SimEventRouter.ts:443 (triggerAbility → plant gesture, contract abilityAnimation.ts:104) + client/src/game/systems/ConstructVfxController.ts:609 (spawnAbilityCastTell at cast site); decoy entity itself: NONE | The decoy — the entire point of the ability — is INVISIBLE: PaperDoubleEntity is simulated (sim/paperDouble.ts) and networked (net/snapshotDelta.ts:179/486), but a full-client grep shows state.paperDoubles is consumed by zero render/scene files; no rig, sprite, or construct draws it. The death burst (World.ts §3c2 ~L6498 → resolveInstantAoeCasts) has no VFX at the burst point — victims just get generic hit-confirmed damage numbers/blasts (SimEventRouter.ts:273-274). Fooled debuff is nameplate-only (statusChips.ts:182). Only cast-time reads ship, and abilityAnimation.ts:99-104's own comment admits 'minimal placeholder... sim correctness, not a full tactile VFX pass'. |
| razor-route | **SHIPPED** | ability-activated (kind="razor-route") for the cast; dash-through on the body-cross; Read-mark tag + window state-driven (readTargetId/readMarkUntilTick via render/markReadPlan.ts) | Cast: SimEventRouter.ts:443 step gesture + ConstructVfxController.ts:609 cast tell (unchanged). Tag-time + window read: StatusVfxController.ts:201-215 planMarkReads → spawnReadMarkSlashes (def :357) — the Interstice slash-pair appears on the crossed enemy THE FRAME the silent World.ts dash-cross write lands (state-driven, no event needed) and holds for the window. Deferred payoff: spawnEmpoweredHitFlourish (ConstructVfxController.ts:537-551). | Track L 2026-07-24: the registry's recorded gap ("contact-site read is not explicit") is closed by the state-driven mark read — the planner sees the tag fields the instant they are written, so the body-cross now produces a visible mark on the target (markReadPlan.test.ts covers the eventless tag-time appearance explicitly). Remaining nuance (recorded): the empowered dash itself still renders as a normal dash; the mark appearing on whoever it crossed is the read. ROUTE chip supplementary. |
| read mark (readTargetId/readMarkUntilTick) | **SHIPPED** | slash-hit (amp consumption at ninja melee, state-driven mark check) + ability-activated kind:"read-mark" (cast); razor-route's eventless dash-cross tag applies the same fields — now visible at tag time via the state-driven window read | window: client/src/game/systems/StatusVfxController.ts:201-215 planMarkReads → spawnReadMarkSlashes (def :357) — paired 45° Interstice-cyan blade slashes at the marked body's shoulders every 240ms for the 5s window, drifting up the body, 300ms expiry fade; payoff: ConstructVfxController.ts:551 spawnEmpoweredHitFlourish (unchanged); cast: rig gesture SimEventRouter.ts:443 + cast tell ConstructVfxController.ts:609 | Track L 2026-07-24: the 5s window now reads on the TARGET in-world (was caster-chip-only); the blade-slash SHAPE separates it from facet's chords under the shared conjured-cyan law (CA2). SimEventRouter's noted "Read-tag flash" fast-follow is superseded by this state-driven read (covers the ability cast AND razor-route's silent tag with no event plumbing). Planner tested (markReadPlan.test.ts). Payoff flourish still shared with undercut (recorded on undercut's own row). |
| recoil-step | **PARTIAL** | ability-activated | SimEventRouter.ts:443 → ProceduralPlayerRig.ts:801 (step gesture, pose at ProceduralPlayerRig.ts:1362-1365) + ConstructVfxController.ts:609 (spawnAbilityCastTell at caster) | Cast reads only via the generic shared floor: authored 'step' rig gesture (abilityAnimation.ts:62 — the map's only negative bodyCommit, a back-lean matching the hop) + class-tinted cast tell at the caster (ConstructVfxController.ts:609) + the sim hop itself (visible displacement, state-driven). No recoil-step-specific VFX (no dust kick/afterimage on the hop), and the rider window (self-knockback reduction via recoilStepUntilTick, read in weapon.ts) has NO read anywhere — no world glow and no statusChips entry either. |
| regen (regenUntilTick/regenHps) | **PARTIAL** | state-driven (nameplate chip only; no SimEvent) | NONE | Nameplate-only: REGEN chip via client/src/game/ui/statusChips.ts:114. The priest buff-aura block in ConstructVfxController.ts:942-953 reads hasteUntilTick and wardAbsorbUntilTick but NOT regenUntilTick, so a regen'd fighter has zero at-site glow; HoT ticks in World.ts:5904-5920 heal silently (no event, no heal number — spawnDamageNumberAt is damage-only). Note applyRegenToAlly (World.ts:1015) currently has no live caller outside tests (substrate only), so the gap is latent rather than player-visible today. |
| resonance window (resonanceUntilTick/resonanceSourceKind) | **PARTIAL** | resonance-triggered (consumption); open window is state-driven and unrendered in-world | client/src/game/render/SimEventRouter.ts:459 audio.play("card",{heavy:true}) + :462 local-only safeShake — audio/camera only, no draw call at the fighter; window-open has NO presenter (not in ConstructVfxController's buff-aura scan :913-954, not in StatusVfxController) | Consumption is non-positional audio + local shake only; SimEventRouter.ts:454-458 comment says the bespoke world-space read is 'still Tier 4 polish, deliberately not built', and eventPresentationRegistry.ts:186-195 self-declares state:'partial' ('Only a generic heavy card audio accent is routed today') against its own lowTierCore 'A distinct resonance glyph/shape'. The 2s open window is the RES chip only (statusChips.ts:86, nameplate-only). |
| return-glass | **SHIPPED** | ability-activated (cast floor) + shield-refunded (World.ts:3491 — additive, emitted only when the refund actually raised charge; full-bar casts stay silent) | client/src/game/systems/StatusVfxController.ts:312-313 → spawnShieldRefundSnap (def :642) — sapphire snap-in ring + rising charge tick at the fighter (the exact read Bastion Pulse and Plant Charge's landing tick share, one presentation for one mechanic family). Plus the unchanged floor: 'guard' rig gesture (abilityAnimation.ts:57) + cast tell (ConstructVfxController.ts:609) + card SFX. | Track L 2026-07-24: closes "ZERO dedicated read anywhere — shield charge only moves on the HUD bar". Sim-tested (legibilityEvents.test.ts: event carries GEO_RETURN_GLASS_SHIELD_REFUND exactly, full-bar cast emits ability-activated but NO shield-refunded). eventPresentationRegistry.ts "shield-refunded" row added (complete). |
| severance | **PARTIAL** | ability-activated (cast); travel state-driven; impact via hit-confirmed | client/src/game/systems/ConstructVfxController.ts:609 (generic cast tell) + client/src/game/render/ProjectileVfx.ts:297 (generic shard) + SimEventRouter.ts:263 (generic hit-confirmed) | Nothing severance-specific exists at the site. The 1300px/s shard renders as a generic straight fire projectile (same look as any priest fire shot); the hit lands as the generic hit-confirmed impact + damage number, plus burn-spark re-cursing from the fire element. No execute/detonate read on the already-cursed target, no polarity/curse-severing visual, and the no-cursed-target dead press produces zero feedback (activated=false emits no event). Only the universal cast tell + 'cut' rig gesture (abilityAnimation.ts:80) mark the cast. |
| shadow-step (Interstice Writ) | **SHIPPED** | ability-activated (x/y = post-blink destination, World.ts:4515-4522); origin = ConstructVfxController's lastPos frame cache (pre-move position, same as every other blink) | client/src/game/systems/ConstructVfxController.ts:857 spawnBlinkStreak (kind-gated at :848, impl LightConstruct.ts:2127) — Interstice-cyan departure dispersal + arrival bloom PLUS the connecting afterimage ribbon (ninja style "slash" passes spawnBlinkStreak's CA5 style-gate: the tether/echo register belongs to Interstice, so shadow-step is the blink that SHOULD wear it); layered on the existing cast tell :609 + rig step gesture SimEventRouter.ts:443 | Track L 2026-07-23: added shadow-step to the same streak path the other four blinks (slip-node/drift-step/plant-charge/bulwark-step) already used — the register's owner was the one blink excluded. Origin→destination causality now reads at both ends. (Blocked press correctly stays silent: activated=false pushes no event.) |
| stolen-fangs lock charges (pendingLockCharges) | **PARTIAL** | state-driven | client/src/game/ui/ActionBarSystem.ts:805-806 (acquired-slot ready ring = stolenFangsCharges/2) + fang glyph :839-845; vitals fed from client/src/game/scenes/OnlineMatchScene.ts:1233 and HangoutScene.ts:1345 (local.pendingLockCharges) | HUD-only (action-bar slot ring — the nameplate-chip class of read, local player only). No world-space read at the fighter when a charge banks (no rig aura/orbiting fangs; grep for pendingLockCharges in render/ and systems/ returns nothing), and the SPEND has no site read either: ProjectileVfx.ts renders homing-pathing shots identically to straight ones (only 'bounce' pathing gets a bespoke tell, ProjectileVfx.ts:254-260). Bank site World.ts:1747-1755 emits no dedicated SimEvent; spend site weapon.ts:435-437 likewise silent. |
| tithe window (titheUntilTick) | **PARTIAL** | ability-activated (kind=crimson-tithe) at cast; emission-leech (World.ts:1933-1942, pushed per leeching hit from proj.leechFraction) carries the payoff | payoff: StatusVfxController.ts:145-150 → spawnLeechThread (L295-335, crimson sagging thread victim→caster, ADD blend) on emission-leech; caster pickup cue SimEventRouter.ts:473-475. Cast: ConstructVfxController.ts:609 cast tell + rig 'weave' gesture (abilityAnimation.ts:47). Window presenter: NONE | Each landed shot during the window draws the world-space crimson leech thread (Drain-axis read, verified: weapon.ts:515 stamps leechFraction while titheActive → World.ts:1924-1946 emits emission-leech), so the payoff is well-presented. But the 3s window itself on the fighter is nameplate-only (TITHE chip statusChips.ts:54 + activeSlots.ts:35), and tithe-stamped projectiles get no crimson tint in ProjectileVfx.ts/ProjectileSystem.ts (grep: leechFraction only appears as a default 0 at ProjectileSystem.ts:843) — in-flight and on-body the window is invisible. |
| unbroken-seal | **SHIPPED** | slash-hit (payoff, + live snapshot sealUntilTick); state-driven (slowedUntilTick → slow rings); ability-activated (cast); armed window state-driven via render/statusWindowPlan.ts | Armed window: client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnSealDiamond (def :394) — four gold ticks forming a contracting diamond outline at the chest (crystal/diamond grammar, the Kindled identity shape — never a halo/cross) every 260ms for the 5s window, 300ms expiry fade. Payoff: ConstructVfxController.ts:551 empowered-hit flourish + StatusVfxController slow drag rings at the victim (unchanged). Cast: SimEventRouter.ts:443 gesture + ConstructVfxController.ts:609 tell. | Track L 2026-07-24: the defining armed state now reads on the paladin's vessel — the seal is visibly charged until spent or expired (expiry fades instead of vanishing). Doctrine #10 fairness: the enemy can see the next Kindled Edge hit is amped BEFORE eating it. Planner tested (statusWindowPlan.test.ts). SEAL chip supplementary. |
| veil (veilUntilTick) | **SHIPPED** | ability-activated (kind=veil-of-nought) / emission-cast — cast; window + break are state-driven (render/veilReadPlan.ts frame-diffs the break-on-firing definedness edge) | client/src/game/systems/StatusVfxController.ts:178-192 → spawnVeilShroud (def :392): quiet desaturated outline-shroud at the body each 300ms while veilUntilTick lives, intensity fading over the final 300ms (expiry tell); spawnVeilBreakSeam (def :420) on the break-on-firing; cast one-shots unchanged (ConstructVfxController.ts:609 + rig 'gather' gesture) | Track L 2026-07-24: verified stale row — duplicate enumeration of veil-of-nought (status-field view); the shroud/seam shipped 2026-07-23 and this row was never refreshed. veilUntilTick is now consumed by StatusVfxController via planVeilRead (headless-tested: window read, natural-expiry silence, break edge consumed once, death never fakes a break). Nameplate VEIL chip + activeSlots ring are supplements, not the read. |
| veil-of-nought | **SHIPPED** | ability-activated (cast); window + break are state-driven — the pure planner render/veilReadPlan.ts frame-diffs the definedness edge (the sim's ONLY clear-to-undefined site is the break-on-firing, so "was live last frame, undefined now" is unambiguous; natural expiry keeps the stale tick and stays silent) | client/src/game/systems/StatusVfxController.ts:154-167 consumes planVeilRead → spawnVeilShroud (def :222) while veiled: a thin desaturated near-white outline ellipse hugging the body, easing inward as it dissolves (white/negative-space register — the vessel unmade, NOT a glow; quietest of the status family: 300ms cadence, 1.4px stroke, 0.3 alpha, intensity fading over the window's final 300ms as the expiry tell) + spawnVeilBreakSeam (def :250) on break: a crisp horizontal seam-snap parting a few px as the veil tears (one-shot pooled bolt) | Track L 2026-07-23: doctrine #10 vs stealth resolved per the audit's design intent — a watching enemy can now see who is veiled and the exact break moment, without a spotlight (Veil's power is homing/satellite blindness, not invisibility from humans). Planner unit-tested headlessly (veilReadPlan.test.ts: full/fading intensity, natural-expiry silence, break edge consumed once, death/respawn never fakes a break, roster hygiene). Cast tell + gather gesture unchanged. E-coupling self-veil (row above) rides the same state-driven shroud for free — the planner reads veilUntilTick regardless of which cast set it. |
| vulnerability (vulnerabilityUntilTick) | **SHIPPED** | state-driven window (render/statusWindowPlan.ts); consumption via hit-confirmed `amped: true` (the radiant amp's status check includes vulnerabilityUntilTick, World.ts:1771-1779 region) | window: client/src/game/systems/StatusVfxController.ts:258-271 planStatusWindows → spawnVulnCrack (def :522) — a small rose crack (two crossed ticks) at a point on the body's edge every 300ms while vulnerable, 300ms expiry fade; consumption: StatusVfxController.ts:315-318 spawnAmpPunishBurst (def :690) at the victim when the amped hit lands | Track L 2026-07-24: both halves now read in-world — the standing "my guard is cracked open" state (was VULN-chip-only) and the punish moment when radiant exploits it (was silently folded into the damage number). Planner + sim tested (statusWindowPlan.test.ts, legibilityEvents.test.ts). Application moment at the pickup still emits no dedicated event (recorded — the window read appearing at the body is the application read; pickup.ts is out of this pass's additive-World.ts scope). |
| bleed-tithe | **SHIPPED** | ability-activated (cast) + emission-leech (leech, World.ts:1934); travel + burn are state-driven (projectiles / burnUntilTick) | client/src/game/render/ProjectileVfx.ts:297 (homing fire shard) + client/src/game/systems/StatusVfxController.ts:72 (burn sparks, def :185) + :145-146 (leech thread, def :295) | Full in-world chain at the site: cast tell + 'weave' rig gesture (generic layer), genuinely-curving homing fire shard drawn state-driven with trail/halo/muzzle, burn sparks cadenced on the cursed victim from burnUntilTick, and the crimson victim-to-caster leech thread on every leeched hit (emission-leech also plays a caster-side pickup cue, SimEventRouter.ts:474). Caveat: the shard is NOT tendril-flagged (only weapon.ts:529/575 basic-fire stamps tendril), so it wears the generic fire-projectile body rather than a bespoke bleed-tithe identity. |
| borrowed-time debt (debtUntilTick/debtAmount) | **SHIPPED** | state-driven — no SimEvent exists for the drain (World.ts:5984-6003 resolves and clears silently); frame-diff on the snapshot is the only tell, and it is wired; cast rides ability-activated kind:"borrowed-time" | client/src/game/systems/ConstructVfxController.ts:894 spawnNovaBurst(SYZ_BORROWED_TIME_DEBT_BURST_RADIUS_PX, SYZYGIST_TINT, "ooze") at the debtor's live position, fired by the :893 consumedThisFrame frame-diff on debtUntilTick (per-player scan covers self-cast AND healed-ally branch); cast moment: rig gesture SimEventRouter.ts:443 via abilityAnimation.ts:81 + cast tell ConstructVfxController.ts:609 | The drain landing — the mechanic's whole world-visible consequence — gets a dedicated, unconditional, at-site burst on whoever the debt resolves on (dedicated radius constant, priest ooze style, deliberately not Interstice's). Remaining gap: the pending ~6s debt window has no world-space read on the body (no aura branch for debtUntilTick in the :913-954 scan), only the crimson DEBT chip (statusChips.ts:192, nameplate-only). |
| bulwark-step | **SHIPPED** | ability-activated (kind "bulwark-step") | client/src/game/systems/ConstructVfxController.ts:853 (spawnBlinkStreak; impl client/src/game/render/LightConstruct.ts:2127) | Dedicated blink read at the site: departure gather-flash + arrival bloom in Kindled gold (origin from lastPos frame cache, destination from the event's x/y). Deliberately no connecting trail — CA5 reserves the afterimage ribbon for slash/ooze styles (LightConstruct.ts ~2138). Plus the universal layer: rig 'step' gesture SimEventRouter.ts:443 (contract abilityAnimation.ts:73), card SFX SimEventRouter.ts:439, cast tell ConstructVfxController.ts:609. Ward survival needs no extra read — the held slab is repainted every frame from shieldActive. |
| burn (burnUntilTick/burnDps) | **SHIPPED** | state-driven (player.burnUntilTick read per frame); DoT ticks additionally emit hit-confirmed | client/src/game/systems/StatusVfxController.ts:72 (spawnBurnSpark, drawn L185-214) — cadenced fire sparks rising off the burning body | Clear at-site read: pooled orange/hot sparks spawn every 80ms at the victim's body while burnUntilTick > tick (STATUS_VFX.fire colors, upward drift + shrink). Wired live in OnlineMatchScene.ts:674/1022 and HangoutScene.ts:344/1274. DoT damage also pops damage numbers + micro-blast at the victim via the hit-confirmed path (SimEventRouter.ts:273-274). |
| crimson-tithe | **SHIPPED** | ability-activated (cast, World.ts:4515) + emission-leech (per-hit payoff, World.ts:1933); window state-driven (titheUntilTick) | client/src/game/systems/StatusVfxController.ts:146 (spawnLeechThread, drawn L295-335) + ConstructVfxController.ts:609 cast tell + SimEventRouter.ts:443 rig weave gesture + SimEventRouter.ts:474 caster pickup cue | The payoff has a clear site read: every tithe-window shot carries leechFraction (weapon.ts:443-446 stamp) so each landed hit emits emission-leech → crimson victim-to-caster thread at the hit site (same machinery as Drain Emission; registry marks emission-leech 'complete'). Caveat: the live 3s window on the fighter between hits is nameplate-only (TITHE chip statusChips.ts:54 + action-bar crimson ring activeSlots.ts:35) — no sustained on-body aura. |
| drain.leechFraction (Drain leech) | **SHIPPED** | emission-leech | client/src/game/systems/StatusVfxController.ts:145-150 -> spawnLeechThread (draw calls :317-327, transientVfx.spawn :329); caster audio at client/src/game/render/SimEventRouter.ts:474 | Crimson victim-to-caster sagging thread (chain-arc geometry re-tinted, LEECH_COLOR 0xdc2626, 260ms, additive) drawn in world space at both endpoints; router adds local-caster 'pickup' cue (SimEventRouter.ts:466-477). eventPresentationRegistry.ts:196-204 marks it state:'complete'. Wired live via OnlineMatchScene.ts:1022 statusVfx.update. |
| drift-step | **SHIPPED** | ability-activated (kind 'drift-step'; event x/y = post-teleport destination, World.ts:4515-4522) | ConstructVfxController blink-streak block -> LightConstruct.spawnBlinkStreak (def LightConstruct.ts:2127) | Origin-to-destination teleport streak at the site: spawnBlinkStreak call at client/src/game/systems/ConstructVfxController.ts:853, kind-gated at :837-841; origin comes from the prev-frame lastPos cache (:852, updated :958-962) since World.ts:4131 hard-sets x/y. Priest 'ooze' style is the one blink of the four (slip-node/drift-step/plant-charge/bulwark-step) that draws a connecting trail body — chassis-axiom CA5 reserves the tether/echo register for Syzygist (comment :824-833). Plus generic cast tell at :609 and rig 'step' gesture (abilityAnimation.ts:88 via SimEventRouter.ts:443). |
| edge-storm | **SHIPPED** | wave-spawned (audio, World.ts:5495) + state-driven projectile-snapshot render (ninjaBladeShard flag) | ProjectileVfx bespoke ninja blade-sliver wave body (+ SimEventRouter audio) | Each empowered swing's wave is a real in-world projectile with a bespoke identity: World.ts:5493 sets wave.ninjaBladeShard; render at client/src/game/render/ProjectileVfx.ts:284 (drawBladeSliverBody, def :808) with cyan NINJA_BLADE_SHARD_GLOW/CORE color override (:180-181, consts :47-48), 0.72 visual radius shrink (:191), muzzle flash at spawn (:207), trail/halo in the same color. Audio: SimEventRouter.ts:514 plays 'shoot' @0.6 on wave-spawned (registry marks the event 'complete', eventPresentationRegistry.ts:290-298). Cast gets the generic cast tell (ConstructVfxController.ts:609). Gap: the buff window + charge bank (edgeStormUntilTick/edgeStormChargesRemaining) has NO sustained on-body read — ninja has no aura branch (ConstructVfxController.ts:921-953 covers wizard/paladin/priest only); STORM chip (statusChips.ts:147) is nameplate-only. |
| emission fire fork — burn ×2 statusScale (cap 3s) | **SHIPPED** | state-driven | client/src/game/systems/StatusVfxController.ts:67-76 (burnUntilTick cadence, 80ms) -> spawnBurnSpark draw at StatusVfxController.ts:185-214 (rising orange/hot sparks at the victim's body via transientVfx.spawn L200); wired live at OnlineMatchScene.ts:674+1021-1022 and HangoutScene.ts:344+1273-1274 | The burn status has a clear at-victim world read (fire sparks for the full authoritative duration). The ×2 statusScale itself is invisible for fire BY DESIGN: EMISSION_BURN_CAP_MS equals the base 3s, so the doubling clamps to no-op (World.ts ~L1885-1891 comment says fire identity reads via impact size + coverage instead). Spark rate/intensity do not scale with burnDps — no 'amplified burn' distinct read, but none is owed given the cap. |
| emission ice fork — freeze ×2 statusScale (cap 2s) | **SHIPPED** | state-driven | client/src/game/systems/StatusVfxController.ts:78-89 (freezeUntilTick cadence, 160ms) -> spawnFreezeShard x2 at StatusVfxController.ts:216-242 + expanding spawnFrostRing at :244-265, all at the victim's position; wired live in both scenes (OnlineMatchScene.ts:1021-1022, HangoutScene.ts:1273-1274) | Clear at-victim world read: paired ice shards drifting outward + expanding frost ring every 160ms while freezeUntilTick holds. The emission doubling (1s -> 2s, genuinely uncapped at 2s per EMISSION_FREEZE_CAP_MS, World.ts ~L1901-1909) reads naturally as the shard/ring emission lasting twice as long; movement halving (freezeMultiplier 0.5) also reads in the victim's gait. No density scaling with statusScale, but the duration read carries it. |
| emission lightning fork — chain half-damage arc | **SHIPPED** | chain-hit | client/src/game/systems/StatusVfxController.ts:139-144 (chain-hit event consumer) -> spawnLightningChainArc at StatusVfxController.ts:337-385: additive 5-point jittered bolt (glow pass L367-371 + core pass L373-377) drawn from event fromX/fromY (victim) to toX/toY (chain target), 130ms life; plus SimEventRouter.ts:374-388 hit cue + hit-stop + local shake | Clear site read connecting both bodies: the sim emits chain-hit with deterministic hit-time positions (World.ts:1996-2005) and the renderer draws the causal bolt exactly along that path. The chain target also gets the full generic hit stack (paired hit-confirmed at World.ts:1987-1993 -> damage number + blast + rig flinch via SimEventRouter hit-confirmed case). Registry classifies chain-hit 'complete' (eventPresentationRegistry.ts:233-241). |
| emission-cast (radial volley + charge dump) | **SHIPPED** | emission-cast | client/src/game/render/SimEventRouter.ts:416-418 (element-carrying heavy card cue + 180ms shake + emissionCastFeel dispatch) -> client/src/game/scenes/OnlineMatchScene.ts:1868-1914: punchZoom toward cast x/y (L1871), local-caster element-tinted cam.flash (L1874), Coptic seal text drawn at the cast site this.add.text(x, y-64, sealText) L1887-1897 with rise/fade tweens L1898-1913 | Clear in-world read at the vessel: the dominant Coptic seal flashes and rises at the cast position (seal grammar = casting grammar, per doctrine #10), camera punches toward the cast, and the radial shard fan itself renders through the normal projectile pipeline with per-shard muzzle flash on first sight (ProjectileVfx.ts:206-208). Registry classifies it 'complete' (eventPresentationRegistry.ts:167-175). Caveat: HangoutScene omits emissionCastFeel, but World.ts gates casts with !hangoutMode so the event can never fire there. |
| facet-break | **SHIPPED** | hit-confirmed (amp payoff) + ability-activated (cast tell) | client/src/game/systems/ConstructVfxController.ts:587 spawnEmpoweredHitFlourish at victimPos (draw def LightConstruct.ts:520) | The amp consumption reads at its true site: on hit-confirmed, if attacker is wizard and facetTargetId === victimId with facetMarkUntilTick live (:570-574), a Geometrician-styled empowered-hit flourish spawns at the victim's position. Cast gets 'mark' rig gesture (abilityAnimation.ts:54) + cast tell (:609). One honest gap: during the 4s mark WINDOW nothing is drawn on the marked target itself — the only sustained read is the caster's MARK chip (statusChips.ts:73, nameplate-only) — but the payoff moment has a clear in-world victim-site read, so this clears SHIPPED. |
| flock-pulse | **SHIPPED** | ability-activated kind 'flock-pulse' (cast-synchronized with pendingInstantAoe, World.ts:3961-4022) | client/src/game/systems/ConstructVfxController.ts:786 (spawnNovaBurst at cast epicenter) | Instant nova at the event's own x/y at full SYZ_FLOCK_PULSE_RADIUS_PX in Syzygist tint/'ooze' style (ConstructVfxController.ts:781-788). Escalated to the 'heavy' presentation tier: heavy 'card' audio, hit-stop, local shake (SimEventRouter.ts:436-448, isBigNova branch). Rig 'pulse' gesture via abilityAnimation.ts:84. Victims read via the generic hit-confirmed damage presentation from resolveInstantAoeCasts (World.ts:4767) plus state-driven slow drag rings at their feet (StatusVfxController.ts:111-121, spawnSlowDragRing :116-117 off slowedUntilTick). |
| focus-hex | **SHIPPED** | state-driven (focusHexTargetId/focusHexMarkUntilTick via entanglementPlan.ts:68-73); amp spend keyed off hit-confirmed | client/src/game/systems/ConstructVfxController.ts:368 (drawTether into tetherLayer) + :587 (spawnEmpoweredHitFlourish at victim) | Full site loop. Persistent priest-to-marked-victim tether redrawn every frame (ConstructVfxController.ts:363-374: planEntanglement :363, drawTether :368, snap/bind bursts :372-373, devotion motes :374; plan logic in entanglementPlan.ts). Amp consumption reads at the victim: on hit-confirmed with focusHexTargetId===victim and mark live, spawnEmpoweredHitFlourish at victim pos (ConstructVfxController.ts:576-587). Cast itself gets the rig 'mark' gesture (SimEventRouter.ts:443 via abilityAnimation.ts:82) + cast-tell burst at caster (ConstructVfxController.ts:609) + 'card' SFX (SimEventRouter.ts:438). Nameplate MARK chip (statusChips.ts:129) is extra, not the read. |
| focus-hex mark (focusHexTargetId/focusHexMarkUntilTick) | **SHIPPED** | state-driven (focusHexMarkUntilTick/focusHexTargetId in snapshot) + hit-confirmed for the amp flourish; cast also gets ability-activated cast tell (ConstructVfxController.ts:609) | ConstructVfxController.ts:366-369 — per-frame drawTether(tetherLayer, t.from, t.to, SYZYGIST_TINT, ENTANGLE_SHAPE, phase) for every pair planned by entanglementPlan.ts:69-71/115-117 (reads focusHexMarkUntilTick > tick from snapshot); bind/snap bursts + devotion motes ConstructVfxController.ts:372-374 (spawnBindBurst/spawnTetherMote); amp payoff spawnEmpoweredHitFlourish at marked victim ConstructVfxController.ts:576-587 on hit-confirmed. Wired: OnlineMatchScene.ts:677 (construct), update :1054 | Full four-beat world read at the site: bind burst where the mark catches, breathing priest→victim tether held every frame on a dedicated off-pool ADD-blend layer (depth 6), victim→priest devotion motes on 150ms cadence, snap burst on release — plus the SYZYGIST-tinted empowered-hit flourish when the amp actually consumes on the marked victim. Caster-side MARK nameplate chip (statusChips.ts:129) is secondary, not the only read. Known v1 simplification: nearest-priest attribution with 2+ priests (entanglementPlan.ts:79-98). |
| freeze (freezeUntilTick/freezeMultiplier) | **SHIPPED** | state-driven (player.freezeUntilTick read per frame) | client/src/game/systems/StatusVfxController.ts:83-85 (2x spawnFreezeShard + spawnFrostRing, drawn L216-265) — ice shards + expanding frost ring at the frozen body | Clear at-site read: every 160ms while freezeUntilTick > tick, two radial ice shards drift outward from the body plus an expanding frost ring centered on the player (STATUS_VFX.ice color). Same live wiring as burn (OnlineMatchScene.ts:1022, HangoutScene.ts:1274). Movement multiplier itself is additionally legible through the actual gait slowdown. |
| ghost-guard | **SHIPPED** | state-driven (frame-diff on ghostGuardChargeUntilTick clearing at the combat.ts tryDeflectDamage consumption — no dedicated SimEvent) | ConstructVfxController.ts (spawnGhostGuardDodge) | Dodge payoff at the evader's body: ConstructVfxController.ts:869-870 consumedThisFrame(ghostGuardWasArmed) -> spawnGhostGuardDodge(pool, pos, INTERSTICE_TINT); bespoke primitive at LightConstruct.ts:2317. Cast reads: spawnAbilityCastTell L609 + rig 'guard' gesture (abilityAnimation.ts:96, via SimEventRouter.ts:443). Note: no continuous in-world aura during the armed window (not in the buff-aura scan at L898+); GHOST chip statusChips.ts:152 is nameplate-only — but cast+payoff site reads are dedicated and clear. |
| hard-aperture | **SHIPPED** | state-driven (wardShellUntilTick > tick, StatusVfxController.ts:94-106); cast moment via ability-activated (cast tell ConstructVfxController.ts:609 + rig guard gesture) | StatusVfxController.ts:102 spawnWardRing — contracting sapphire rings (drawn at StatusVfxController.ts:267-290, WARD_COLOR 0x38bdf8) pulse at the fighter every 130ms (WARD_RING_INTERVAL_MS, :17) the whole time the shell lives | Continuous in-world shell read at the vessel ('so attackers can SEE why their damage halved', file comment :91-93). Nameplate WARD chip (statusChips.ts:51) and action-bar windowFrac (activeSlots.ts:40-41) are HUD extras on top. Gap worth knowing: the halving moment itself (World.ts:1678-1680 EMISSION_WARD_DAMAGE_MULT) emits no bespoke absorb event — the persistent ring is the only tell that a specific hit got halved. |
| haste-gift | **SHIPPED** | state-driven (hasteUntilTick per-player scan); plus ability-activated for the cast tell | ConstructVfxController priest buff-aura layer -> LightConstruct.drawBuffAura (def LightConstruct.ts:2025) | Continuous cool-white 'ooze' aura drawn at the buffed fighter's body every frame: draw call at client/src/game/systems/ConstructVfxController.ts:951 (drawBuffAura on auraLayerPriest), gated by p.hasteUntilTick > tick at :944, fading over the final 300ms. The scan covers EVERY player's own field, so applyHasteToAlly's cross-player write reads at the ALLY, not the caster (comment :898-908). Cast also gets the generic class cast tell (spawnAbilityCastTell, ConstructVfxController.ts:609, on ability-activated) and the rig 'weave' gesture (SimEventRouter.ts:443 via abilityAnimation.ts:87). HASTE chip at statusChips.ts:115 is nameplate-only extra, not the primary read. Wired live: OnlineMatchScene.ts:677/1054. |
| lattice | **SHIPPED** | ability-activated (kind 'lattice') + client-side zone timer | client/src/game/systems/ConstructVfxController.ts:813 drawGroundField per-frame (draw def LightConstruct.ts:2241-2294, 'shatter' style) | Persistent ground-field at the cast point: crystal-facet plane with broken (non-halo) boundary ring, facet seam triangulation, and upward-drifting crystal dust, redrawn every frame on an additive latticeLayer (:289-292). Zone pushed from the cast event (:794-803) and ticked locally; radius/duration come from the SAME sim constants the DoT zone uses (GEO_LATTICE_ZONE_RADIUS_PX=150 / GEO_LATTICE_ZONE_DURATION_MS=2200, sim/constants.ts:122-123), so the visual footprint matches the real hazard. Deliberately does not read state.firePatches (comment :790-793). Cast also gets 'plant' rig gesture (abilityAnimation.ts:56) + cast tell. |
| mystery.denyAscension (Mystery ascension denial) | **SHIPPED** | player-killed (denial derived from killer's snapshot entry at event time) | client/src/game/render/deathFxPainter.ts:195-210 (UNMAKING: strokeCircle :199 contracting void ring + 6 inward crush rays :201-209) with denied palette swap :48-51; derivation at client/src/game/render/renderContract.ts:602-603 | Single-derivation doctrine honored: renderContract.ts:602 calls resolveEmission(resolvePlayerBuild(killer)).mystery.denyAscension — same derivation the sim uses. Denied soul gets void palette, a contracting void ring that crushes the mote out with inward rays ('the crime, made watchable'), never sets absorbT so the golden motif-absorption bloom/reception rings are suppressed (deathFxPainter.ts:46-47, :81, :178; denied envelope renderContract.ts:718), shorter total life (renderContract.ts:701 DENIED_TOTAL_MS). Kill pop preserved for killer feedback (:59). Wired: noteDeathEvents OnlineMatchScene.ts:1580, drawDeathFx :2255. |
| mystery.wrapShots (Mystery wrap) | **SHIPPED** | state-driven (projectile snapshot positions; wrapShots flag itself unread by render code) | client/src/game/render/ProjectileVfx.ts:234-245 (trail discontinuity guard inside the trail draw loop; the break is the early-return at :241-244) | Doctrine #10's requirement — trail must BREAK at the seam — is implemented: any inter-frame segment >200px is treated as a teleport and the trail restarts clean on the far edge instead of smearing a screen-wide hitscan streak; comment explicitly cites 'wrap-flagged shard (six-axes Mystery)'. Caveats: negative-space read only (no positive exit/entry seam flash), and the guard is a pure distance heuristic — it never reads proj.wrapShots (wasm bridge round-trips the flag at worldStateBridge.ts:1041/1202 but no renderer consumes it). |
| needle | **SHIPPED** | state-driven projectile-snapshot render (ninjaBladeShard flag); ability-activated (cast tell + rig thrust) | ProjectileVfx bespoke blade-sliver shard body | The fast shard is a real projectile with a bespoke at-site body: World.ts:4223 sets shard.ninjaBladeShard = true (spawn :4204-4215); rendered via drawBladeSliverBody at client/src/game/render/ProjectileVfx.ts:284 with the cyan color override (:180-181) plus muzzle flash on first sight (:207) — deliberately distinct from the wizard's crystal dart (comment :277-284). Cast gets the generic class cast tell (ConstructVfxController.ts:609, fired at post-lunge x/y since World.ts:4515-4522 pushes nextEntity.x/y) and the rig 'thrust' gesture (abilityAnimation.ts:92 via SimEventRouter.ts:443). Gap: the auto-target self-lunge is an instant position hard-set (World.ts:4198-4202) NOT covered by the blink-streak block (ConstructVfxController.ts:837-841 lists only slip-node/drift-step/plant-charge/bulwark-step), so the body snap up to NINJA_NEEDLE_LUNGE_PX has no origin-to-destination streak. |
| overclock | **SHIPPED** | state-driven (overclockUntilTick > tick, ConstructVfxController.ts:922); cast moment via ability-activated (cast tell :609 + rig 'gather' gesture, abilityAnimation.ts:59) | ConstructVfxController.ts:924 drawBuffAura on auraLayerWizard — continuous crystal-cyan 'shatter'-style aura at the fighter (anchored at pos.y - HAND_RAISE*0.7, :919) every frame while the window lives, fading over the final 300ms (:912, :923) | Dedicated sustained world read at the site, scanned per-player off the snapshot so spectators/opponents see it too. OVCK nameplate chip (statusChips.ts:77) and activeSlots windowFrac (activeSlots.ts:44-45) are HUD-side duplicates, not the lead read. |
| plant-charge | **SHIPPED** | ability-activated (kind "plant-charge"; event x/y = teleport DESTINATION, origin from CVC's lastPos cache — see CVC:824-833) + shield-refunded (World.ts:3810 at the landing write — Track L 2026-07-24, closes this row's recorded refund gap) | ConstructVfxController blink-streak block → spawnBlinkStreak(pool, from(lastPos), dest, KINDLED_TINT) at client/src/game/systems/ConstructVfxController.ts:853 (kind-gated at :839; draw impl LightConstruct.ts:2127); landing ward-ready tick: StatusVfxController.ts:312-313 spawnShieldRefundSnap at the LANDING point (def :642, silent when the bar was already full); cast beat = rig "step" gesture via SimEventRouter.ts:443 w/ contract abilityAnimation.ts:69, card SFX SimEventRouter.ts:438 | The core verb (short aim-directed teleport) has a clear site read: mote streak from origin to landing in paladin gold (no connecting trail body by design — CA5 reserves the afterimage register for Interstice/Syzygist, CVC:829-833) plus the authored "step" pose on the vessel. Track L 2026-07-24: the KIN_PLANT_CHARGE_SHIELD_REFUND tick now reads at the landing via the shared shield-refunded presentation (one read for one mechanic family with Return Glass/Bastion Pulse). Remaining recorded deferral: the doc's "ends in ward-ready stance" POSE (World.ts:3611-3613) — rig-side, melee-loop-owned. |
| prism-fan | **SHIPPED** | ability-activated (kind 'prism-fan') | client/src/game/systems/ConstructVfxController.ts:773 spawnCrystalShards (draw def LightConstruct.ts:1745-1808) | Dedicated cast read at the caster: a cone of 7 faceted crystal diamonds fanning out along the caster's aim (spread 0.9 rad, animated outward over 320ms) plus a palm conjure-flash at origin — oriented from caster.aim (:772) so the visual cone matches resolveInstantAoeCasts' cone check direction. Also gets the wizard 'fan' rig gesture (abilityAnimation.ts:55), generic cast tell (:609), and 'card' SFX (SimEventRouter.ts:438). Victims hit by the cone get standard hit-confirmed presentation. |
| rally-light | **SHIPPED** | ability-activated for the cast; sustained aura is state-driven (rallyLightUntilTick, per-player scan every frame) | ConstructVfxController buff-aura scan → drawBuffAura(auraLayerPaladin, KINDLED_TINT, "seal", radius 30) at client/src/game/systems/ConstructVfxController.ts:940 (window read + dedicated radius bump at :929-931 — "a shared-radius buff reads bigger than a personal one"; draw impl LightConstruct.ts:2025); cast beat = rig "gather" gesture via SimEventRouter.ts:443 w/ contract abilityAnimation.ts:71, card SFX SimEventRouter.ts:438 | The aura-source window has a clear, continuously-drawn site read on the caster, and rally-light is the only paladin buff with a distinguishing visual parameter (radius 30 vs 22). Real gaps to log: the drawn 30px ring is decorative — the actual KIN_RALLY_LIGHT_RADIUS_PX beneficiary radius (World.ts:891) is never visualized; allies inside it receiving rallyLightDamageMultiplier/KIN_RALLY_LIGHT_MOVE_MULTIPLIER (World.ts:903, :2528-2529) get zero read that they are buffed (no aura, no chip — no rallyLight row in statusChips.ts); and when aegis/kindled-resolve overlap, the three windows collapse into one ring. |
| read-mark | **SHIPPED** | ability-activated (cast) + slash-hit (payoff); mark window itself state-driven (readTargetId/readMarkUntilTick) | ConstructVfxController.ts (empowered-hit flourish + cast tell) + SimEventRouter.ts rig gesture | Payoff read at the victim: ConstructVfxController.ts:551 spawnEmpoweredHitFlourish on slash-hit, gated L537-539 on attacker.readTargetId===victimId && readMarkUntilTick>tick. Cast reads at caster: spawnAbilityCastTell ConstructVfxController.ts:609 + rig 'mark' gesture SimEventRouter.ts:443 / abilityAnimation.ts:93. Gap: no persistent world-space mark ON the target during the 5s window — the MARK chip (statusChips.ts:142) is caster-side nameplate-only. Controller live via OnlineMatchScene.ts:677/1054. |
| second-wind | **SHIPPED** | state-driven (frame-diff on secondWindUntilTick clearing when the next NINJA melee arc-hit lands — no dedicated SimEvent) | ConstructVfxController.ts (self-directed nova burst) | Heal/energy payoff at the caster: ConstructVfxController.ts:876-877 consumedThisFrame(secondWindWasArmed) -> spawnNovaBurst(pos, NINJA_SECOND_WIND_BURST_RADIUS_PX, INTERSTICE_TINT, 'slash'). Deliberately NOT in the victim-side empowered-hit check — its window clears in the same tick it's consumed (race documented L529-534), hence the self-directed deferred-payoff path. Cast reads: spawnAbilityCastTell L609 + rig 'gather' gesture (abilityAnimation.ts:97). WIND chip statusChips.ts:154 nameplate-only supplement. |
| self-lattice | **SHIPPED** | state-driven (wardAbsorbUntilTick) for presence; syz-ward-absorbed for spend | client/src/game/systems/ConstructVfxController.ts:951 (drawBuffAura on the caster while pool lives) + client/src/game/scenes/OnlineMatchScene.ts:1838 (absorb blast on spend) | Continuous Syzygist 'ooze' aura pulses at the caster's body while the ward pool is live — state-driven scan of wardAbsorbUntilTick, priest branch (ConstructVfxController.ts:942-952). Spend reads at the site: syz-ward-absorbed routes to spawnSyzygistWardAbsorbFlash (SimEventRouter.ts:562) → spawnExplosionBlastBig at the protected body, 48px on break vs 30px (OnlineMatchScene.ts:1838-1842), plus heavy-tier hit-stop/shake with break escalation (SimEventRouter.ts:567-577). Cast gets rig 'guard' gesture (abilityAnimation.ts:85) + cast tell (ConstructVfxController.ts:609). Nameplate WARD chip (statusChips.ts:125) is extra. |
| severing-answer | **SHIPPED** | parry-deflected + hit-confirmed (consumption, World.ts:1662-1675); ability-activated (cast); window state-driven (counterUntilTick) | client/src/game/render/SimEventRouter.ts:331 (rig.triggerParryFlash — white arc overdrive + expanding impact ring, ProceduralPlayerRig.ts:837-839) + SimEventRouter.ts:330 parry audio + heavy hit-stop L333; returned damage lands as damage number + blast at attacker via SimEventRouter.ts:273-274 | The consumption moment has a real heavy-tier world read at the stancer (parry flash + hit-stop + audio) and a genuine damage read at the punished attacker (hit-confirmed with attackerId = stancer). Caveats: it borrows the ordinary parry vocabulary 1:1 (no bespoke severing identity in the flash), and the 0.5s stance window itself is nameplate-only (CNTR chip statusChips.ts:58 + activeSlots.ts:39) — attackers get no in-world stance tell before shooting into it, beyond the generic cast tell + 'cut' gesture at press. |
| shard-ring | **SHIPPED** | ability-activated (kind="shard-ring") | ConstructVfxController.ts (nova burst) + SimEventRouter.ts (heavy audio/hit-stop/shake) | In-world AOE read at the epicenter: ConstructVfxController.ts:784 spawnNovaBurst({x:ev.x,y:ev.y}, NINJA_SHARD_RING_RADIUS_PX, INTERSTICE_TINT, 'slash') — event x/y IS the sim epicenter (cast-synchronized with pendingInstantAoe, comment L776-780). SimEventRouter.ts:436-447 grants heavy-tier audio.play('card',{heavy}) + holdHitStop + local shake; rig 'pulse' gesture via abilityAnimation.ts:94. Nova primitive at LightConstruct.ts:1827. |
| shelter-seal (Shelter Writ) | **SHIPPED** | state-driven (wardShellUntilTick > tick) | client/src/game/systems/StatusVfxController.ts:102 spawnWardRing (draw def :267-290) | Sapphire ward ring pulses at the vessel every 130ms (WARD_RING_INTERVAL_MS :17) while the shell lives; ring CONTRACTS inward (:277 'a shell holding, not a blast leaving') so it reads distinct from the expanding frost ring — attackers can see why damage halved. Cast moment additionally gets the paladin 'guard' rig gesture (SimEventRouter.ts:443 -> abilityAnimation.ts:48), the generic cast tell (ConstructVfxController.ts:609 spawnAbilityCastTell), and the 'card' SFX (SimEventRouter.ts:438). Nameplate WARD chip (statusChips.ts:51) exists but is supplementary, not the only read. |
| shock-ring | **SHIPPED** | ability-activated for the hop/cast; the landing slam has NO SimEvent — state-driven frame-diff (field defined→undefined is the only tell, CVC:856-863) | Landing slam: ConstructVfxController deferred-payoff frame-diff → spawnNovaBurst(pos, KIN_SHOCK_RING_RADIUS_PX, KINDLED_TINT, "seal") at client/src/game/systems/ConstructVfxController.ts:880 (detect :879 via consumedThisFrame on shockRingArmedUntilTick; draw impl LightConstruct.ts:1827) + heavy hit-stop/local shake via onDeferredNovaImpact (OnlineMatchScene.ts:1047-1053, wired :1062). Cast beat = rig "pulse" gesture via SimEventRouter.ts:443 w/ contract abilityAnimation.ts:70, card SFX SimEventRouter.ts:438 | Full site presentation: sim hop itself is visible motion, cast pose fires on press, and the slam draws a nova ring at the paladin at the REAL AOE radius (KIN_SHOCK_RING_RADIUS_PX) exactly when World.ts's landing branch (~L2644→L2649) consumes the arm window, backed by "heavy"-tier hit-stop + shake. Only gap: the slam is silent — onDeferredNovaImpact is camera-only, no audio.play (consistent with the never-synthesize-audio hard rule; victims still get generic hit-confirmed audio). |
| slip-node | **SHIPPED** | ability-activated (World.ts:4515-4522 pushes it AFTER the teleport case, so x/y = the landing point) | ConstructVfxController.ts:853 spawnBlinkStreak — departure dispersal flash at the origin (LightConstruct.ts:2198 spawnAbilityCastTell reuse) + heavier arrival glow bloom at the destination (LightConstruct.ts:2202-2209); origin from the lastPos frame cache (:852), destination from event x/y | Clear both-ends world read. Deliberately NO connecting trail body for wizard — spawnBlinkStreak style-gates the ribbon to slash/ooze (LightConstruct.ts:2137, CA5: tether/echo is Interstice+Syzygist canon only), so Geometrician gets departure+arrival bursts. Known v2 gap the sim itself flags: the design doc's lingering 'fading node enemies can read' marker is not built (World.ts:3395-3397 comment). |
| slow (slowedUntilTick/slowMultiplier) | **SHIPPED** | state-driven (player.slowedUntilTick read per frame); the player-slowed SimEvent exists but SimEventRouter deliberately no-ops it (SimEventRouter.ts:365-367) | client/src/game/systems/StatusVfxController.ts:116-117 (spawnSlowDragRing x2, drawn L161-183) — paired contracting foot-level drag-wake rings | Clear at-site read hugging the feet: every 220ms while slowedUntilTick > tick, two opposed-phase ellipse rings (SLOW_COLOR 0x7dd3fc) drag inward at foot level — designed as a non-colour-only 'drag wake' per the L108-110 comment. eventPresentationRegistry.ts:120-128 records this as the player-slowed presentation and it is wired live (OnlineMatchScene.ts:1022, HangoutScene.ts:1274). HUD chip + actual gait reduction are supplementary channels, not the primary read. |
| sunlance | **SHIPPED** | shot-fired + state-driven (sunlanceUntilTick) | client/src/game/systems/ConstructVfxController.ts:735 spawnLance (draw def client/src/game/render/LightConstruct.ts:1097-1183) | Every wizard shot fired during the window projects a visibly longer crystal lance from the rig's firing hand: reach base 46 -> 78px (LANCE_REACH_SUNLANCE, :106-109, selected at :706-709), further scaled by channel ramp (:734). This path is live because the wizard's gun is delivery 'projectile' (weapons.ts:98-101, :219), so ev.hitscanHits is absent and the sunlance base applies; caveat: if a card ever resolves the build to raycast, reach comes from the hit point (:731-733) and the sunlance differential disappears. Cast gets 'thrust' rig gesture (abilityAnimation.ts:53) + cast tell (:609). SUN nameplate chip (statusChips.ts:68) is supplementary. |
| sunspike | **SHIPPED** | state-driven (snapshot projectiles, proj.kindledThrust flag via renderContract.ts:145); cast beat via ability-activated | ProjectileVfx.ts:290 (drawSpikeBody, drawn ProjectileVfx.ts:851-880) + trail/halo in KINDLED_THRUST_GLOW (ProjectileVfx.ts:180-184) + muzzle flash (ProjectileVfx.ts:207) | Dedicated in-world identity: World.ts sets shard.kindledThrust=true, routed to a bespoke solid symmetric gold spike body (drawSpikeBody, deliberately not a recolor of the ninja sliver) with gold trail/halo and a spawn muzzle flash, full-size. Cast side gets the authored 'thrust' rig gesture (abilityAnimation.ts:65) + cast tell. Hit lands through the generic hit-confirmed pipeline (damage number + blast, SimEventRouter.ts:263-280) — no sunspike-specific impact read, but the projectile itself is a clear, distinct site presentation. |
| undercut | **SHIPPED** | slash-hit (payoff flourish, gated by live undercutUntilTick snapshot check) + ability-activated (cast tell) | ConstructVfxController empowered-hit flourish -> LightConstruct.spawnEmpoweredHitFlourish (def LightConstruct.ts:520) | Payoff read at the victim's body: spawnEmpoweredHitFlourish call at client/src/game/systems/ConstructVfxController.ts:551, gated at :536 by attacker.undercutUntilTick > tick on each slash-hit. Safe from the Second-Wind same-tick-clear race because the window is non-consuming — World.ts:5312-5322 explicitly does 'no clearing here' at the arc-hit execute site (:5316). Cast gets the generic cast tell (:609) + rig 'cut' gesture (abilityAnimation.ts:90). Caveats: the flourish fires on ANY landed slash while the window lives (it does not verify the sub-threshold execute actually triggered — that moment reads only via the generic player-killed cinematic) and the visual is shared with Read Mark; sustained on-body window read is absent (no ninja aura branch, :921-953) — UNDER chip (statusChips.ts:145) is nameplate-only. |
| wall-bloom | **SHIPPED** | state-driven (frame-diff on wallBloomUntilTick clearing — no dedicated SimEvent, comment L856-863); cast via ability-activated | ConstructVfxController.ts (deferred-payoff nova) + OnlineMatchScene onDeferredNovaImpact | Wall-kick burst at the kicker's body (= wall-contact site): ConstructVfxController.ts:884 spawnNovaBurst(pos, NINJA_WALL_BLOOM_RADIUS_PX, INTERSTICE_TINT, 'slash') when consumedThisFrame(wallBloomWasArmed) fires (L883), plus onDeferredNovaImpact callback L885 → OnlineMatchScene.ts:1047 for scene-side feel. Cast gets spawnAbilityCastTell L609 + rig 'plant' gesture (abilityAnimation.ts:95). Armed-window BLOOM chip (statusChips.ts:149) is nameplate-only supplement. |
| ward-absorb (wardAbsorbUntilTick/wardAbsorbRemaining) | **SHIPPED** | syz-ward-absorbed (spend/break beat); state-driven for the sustained window aura | client/src/game/scenes/OnlineMatchScene.ts:1838 (spawnExplosionBlastBig, cool-white 0xdbeafe, 30px / 48px on wardBroke, at the protected player; caster source-pulse at :1846) dispatched from SimEventRouter.ts:555-578 with heavy-tier hit-stop+shake; sustained window: ConstructVfxController.ts:947-951 aura | The spend moment is a clear at-site read: syz-ward-absorbed (emitted by combat.ts trySyzygistWard ~L187) → SimEventRouter.ts:562 spawnSyzygistWardAbsorbFlash → OnlineMatchScene.ts:1838 world-space blast on the protected vessel (expanded 48px on wardBroke so depletion reads), plus caster source pulse (:1846) and real 'heavy' budget hit-stop (SimEventRouter.ts:567-568, 1.4x on break) and shake (:573-576); deliberately silent audio (never-synthesize rule). Caveats recorded: the SUSTAINED window read is the shared priest ooze aura (ConstructVfxController.ts:947-951) — indistinguishable from haste and class-gated to priest holders, so a glass-ward on a non-priest ally has no idle tell until hit; nameplate WARD chip statusChips.ts:125 covers that gap nameplate-only. Do not confuse with wardShellUntilTick (StatusVfxController.ts:94-106 sapphire rings) or paladin ward-absorbed/team-peel-absorbed (ConstructVfxController.ts:679-689) — different mechanics. |
| ward.fieldMs (Ward shell, wardShellUntilTick) | **SHIPPED** | state-driven | client/src/game/systems/StatusVfxController.ts:94-106 (reads player.wardShellUntilTick > state.tick) -> spawnWardRing :267-290 (setStrokeStyle :272, transientVfx.spawn :278) | Sapphire (0x38bdf8) rings pulse every 130ms around the vessel and contract INWARD (1.4->0.9 scale, deliberately inverse of the expanding frost ring) for the shell's whole life — 'so attackers can SEE why their damage halved'. No additional per-hit flash at the resolveRangedHit x0.5 consumption moment, but the persistent site read covers it; a nameplate WARD chip exists as a supplementary channel only. |
