# Copy values from `npx convex dev` into client/.env.local for browser testing.
# VITE_CONVEX_URL=https://your-dev-deployment.convex.cloud

# Optional LAN display override for the Host client panel.
# VITE_PUBLIC_HOST_ADDRESS=192.168.1.20
# VITE_PUBLIC_HOST_PORT=5173
